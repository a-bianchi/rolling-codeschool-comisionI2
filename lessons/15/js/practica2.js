var nombre = "Alejo";

// Definir una funcion
function Funcion() {
  console.log("Hola Chicos");
}

// Definir una funcion con parametros
function Saludo(nombre) {
  console.log("Hola " + nombre);
}

// Definir una funcion con parametros
function SumaDosNumero(numero1, numero2) {
  return numero1 + numero2;
}

// Definir una funcion retorno
function RetornaNombre() {
  return "Martin";
}

// Saludo("Martin");
// Saludo("Alejo");

// var resulta = SumaDosNumero(1, 1);
// var resta = resulta - 1;
// var unNombre = RetornaNombre();
// console.log(unNombre);

function Login(usuario) {
  // Hace el login de usaurio
  // Asignar una imagen de perfil predeterminada
  // Si no tiene email le va asignar uno de la empresa
  return true;
}

function AsignacionImagens(usuario) {
  // Asignar una imagen de perfil predeterminada
  return true;
}

var usuario = { nombre: "Alejo" };
Login(usuario);
AsignacionImagens(usuario);
